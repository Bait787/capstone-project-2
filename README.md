# Installation & Setup

To install, navigate to the capstone-project-final directory and run the following to open it on localhost:

npm install
npm run dev

# General

Once running there is a built in help page in the navigation bar that explains each component in more detail
